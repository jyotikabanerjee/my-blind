import React from 'react';
import PropTypes from 'prop-types';

const BlContent = (props) => {
    return (
        <div className="flex flex-row">
            Content
        </div>
    );
};

BlContent.propTypes = {};
BlContent.defaultProps = {};

export default BlContent;
